export class CVendors {

    constructor(public vendor_id: number,
                public vendor_name: string,
                public vendor_email: string,
                public vendor_password: string,
            ){ }
}